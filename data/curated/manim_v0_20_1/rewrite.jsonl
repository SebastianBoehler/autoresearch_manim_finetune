{"case_id": "equation_highlight", "tags": ["text", "algebra", "tier:gold", "source:local"], "entry_scene": "EquationHighlight", "prompt": "Make a Manim scene that writes the quadratic formula, then highlights the discriminant term b^2 - 4ac with a surrounding rectangle and caption.", "must_contain": ["MathTex(", "SurroundingRectangle(", "Text(", "FadeIn("], "must_not_contain": ["Axes("], "completion": "from manim import *\n\nclass EquationHighlight(Scene):\n    def construct(self):\n        formula = MathTex(\n            r\"x = \\\\frac{-b \\\\pm \\\\sqrt{b^2 - 4ac}}{2a}\",\n            font_size=54,\n        )\n        caption = Text(\"The discriminant controls the roots\", font_size=28).next_to(formula, DOWN, buff=0.8)\n        box = SurroundingRectangle(formula[0][12:20], color=YELLOW, buff=0.1)\n\n        self.play(Write(formula))\n        self.play(Create(box), FadeIn(caption))\n        self.wait(1)\n", "license": "MIT", "source_repo_path": "data/manim_seed_cases.json", "curation_decision": "rewrite", "curation_reason": "fails to render on Manim 0.20.1 because the MathTex string contains doubled LaTeX escapes"}
{"case_id": "docs_vector_arrow", "prompt": "Build a Manim scene showing the origin, a vector arrow from (0, 0) to (2, 2), and text labels for both coordinates on a NumberPlane.", "completion": "from manim import *\n\nclass VectorArrow(Scene):\n    def construct(self):\n        dot = Dot(ORIGIN)\n        arrow = Arrow(ORIGIN, [2, 2, 0], buff=0)\n        numberplane = NumberPlane()\n        origin_text = Text('(0, 0)').next_to(dot, DOWN)\n        tip_text = Text('(2, 2)').next_to(arrow.get_end(), RIGHT)\n        self.add(numberplane, dot, arrow, origin_text, tip_text)", "entry_scene": "VectorArrow", "tags": ["docs", "vector", "geometry", "plane", "tier:gold", "source:docs", "duration:5s"], "must_contain": ["Dot(", "Arrow(", "NumberPlane(", "Text("], "must_not_contain": ["BarChart("], "source_name": "VectorArrow", "source_url": "https://docs.manim.community/en/stable/examples.html", "source_anchor": "vectorarrow", "license": "MIT", "target_duration_seconds": 5, "target_duration_tolerance_seconds": 2, "source_repo_path": "data/manim_docs_seed_cases.jsonl", "curation_decision": "rewrite", "curation_reason": "exact Manim 0.20.1 render produced only a still frame with zero animations; rewrite with a deliberate explanatory animation beat"}
{"case_id": "docs_sin_and_cos_plot", "prompt": "Create a Manim plotting scene with axes that draws both the sine and cosine curves, labels them, and highlights the graphing setup clearly.", "completion": "from manim import *\n\nclass SinAndCosFunctionPlot(Scene):\n    def construct(self):\n        axes = Axes(\n            x_range=[-10, 10.3, 1],\n            y_range=[-1.5, 1.5, 1],\n            x_length=10,\n            axis_config={\"color\": GREEN},\n            x_axis_config={\n                \"numbers_to_include\": np.arange(-10, 10.01, 2),\n                \"numbers_with_elongated_ticks\": np.arange(-10, 10.01, 2),\n            },\n            tips=False,\n        )\n        axes_labels = axes.get_axis_labels()\n        sin_graph = axes.plot(lambda x: np.sin(x), color=BLUE)\n        cos_graph = axes.plot(lambda x: np.cos(x), color=RED)\n\n        sin_label = axes.get_graph_label(\n            sin_graph, \"\\\\sin(x)\", x_val=-10, direction=UP / 2\n        )\n        cos_label = axes.get_graph_label(cos_graph, label=\"\\\\cos(x)\")\n\n        vert_line = axes.get_vertical_line(\n            axes.i2gp(TAU, cos_graph), color=YELLOW, line_func=Line\n        )\n        line_label = axes.get_graph_label(\n            cos_graph, r\"x=2\\pi\", x_val=TAU, direction=UR, color=WHITE\n        )\n\n        plot = VGroup(axes, sin_graph, cos_graph, vert_line)\n        labels = VGroup(axes_labels, sin_label, cos_label, line_label)\n        self.add(plot, labels)", "entry_scene": "SinAndCosFunctionPlot", "tags": ["docs", "plot", "axes", "trigonometry", "tier:gold", "source:docs", "duration:10s"], "must_contain": ["Axes(", ".plot(", "MathTex("], "must_not_contain": ["ThreeDScene"], "source_name": "SinAndCosFunctionPlot", "source_url": "https://docs.manim.community/en/stable/examples.html", "source_anchor": "sinandcosfunctionplot", "license": "MIT", "target_duration_seconds": 10, "target_duration_tolerance_seconds": 3, "source_repo_path": "data/manim_docs_seed_cases.jsonl", "curation_decision": "rewrite", "curation_reason": "required MathTex API contract is absent from the completion"}
{"case_id": "docs_argmin_example", "prompt": "Create a Manim scene that plots a convex function, tracks a moving point on the curve with a ValueTracker, and marks the argmin with a dashed line and label.", "completion": "from manim import *\n\nclass ArgMinExample(Scene):\n    def construct(self):\n        ax = Axes(\n            x_range=[0, 10], y_range=[0, 100, 10], axis_config={\"include_tip\": False}\n        )\n        labels = ax.get_axis_labels(x_label=\"x\", y_label=\"f(x)\")\n\n        t = ValueTracker(0)\n\n        def func(x):\n            return 2 * (x - 5) ** 2\n        graph = ax.plot(func, color=MAROON)\n\n        initial_point = [ax.coords_to_point(t.get_value(), func(t.get_value()))]\n        dot = Dot(point=initial_point)\n\n        dot.add_updater(lambda x: x.move_to(ax.c2p(t.get_value(), func(t.get_value()))))\n        x_space = np.linspace(*ax.x_range[:2],200)\n        minimum_index = func(x_space).argmin()\n\n        self.add(ax, labels, graph, dot)\n        self.play(t.animate.set_value(x_space[minimum_index]))\n        self.wait()", "entry_scene": "ArgMinExample", "tags": ["docs", "plot", "tracker", "optimization", "tier:gold", "source:docs", "duration:20s"], "must_contain": ["Axes(", "ValueTracker(", "DashedLine(", "MathTex("], "must_not_contain": ["ThreeDScene"], "source_name": "ArgMinExample", "source_url": "https://docs.manim.community/en/stable/examples.html", "source_anchor": "argminexample", "license": "MIT", "target_duration_seconds": 15, "target_duration_tolerance_seconds": 4, "source_repo_path": "data/manim_docs_seed_cases.jsonl", "curation_decision": "rewrite", "curation_reason": "required DashedLine and MathTex API contracts are absent from the completion"}
{"case_id": "docs_graph_area_plot", "prompt": "Create a Manim graph scene with two plotted curves, vertical guide lines, labeled x-values, and shaded areas between the curves over two intervals.", "completion": "from manim import *\n\nclass GraphAreaPlot(Scene):\n    def construct(self):\n        ax = Axes(\n            x_range=[0, 5],\n            y_range=[0, 6],\n            x_axis_config={\"numbers_to_include\": [2, 3]},\n            tips=False,\n        )\n\n        labels = ax.get_axis_labels()\n\n        curve_1 = ax.plot(lambda x: 4 * x - x ** 2, x_range=[0, 4], color=BLUE_C)\n        curve_2 = ax.plot(\n            lambda x: 0.8 * x ** 2 - 3 * x + 4,\n            x_range=[0, 4],\n            color=GREEN_B,\n        )\n\n        line_1 = ax.get_vertical_line(ax.input_to_graph_point(2, curve_1), color=YELLOW)\n        line_2 = ax.get_vertical_line(ax.i2gp(3, curve_1), color=YELLOW)\n\n        riemann_area = ax.get_riemann_rectangles(curve_1, x_range=[0.3, 0.6], dx=0.03, color=BLUE, fill_opacity=0.5)\n        area = ax.get_area(curve_2, [2, 3], bounded_graph=curve_1, color=GREY, opacity=0.5)\n\n        self.add(ax, labels, curve_1, curve_2, line_1, line_2, riemann_area, area)", "entry_scene": "GraphAreaPlot", "tags": ["docs", "plot", "area", "axes", "tier:gold", "source:docs", "duration:20s"], "must_contain": ["Axes(", ".plot(", "get_area(", "get_vertical_line("], "must_not_contain": ["ThreeDAxes("], "source_name": "GraphAreaPlot", "source_url": "https://docs.manim.community/en/stable/examples.html", "source_anchor": "graphareaplot", "license": "MIT", "target_duration_seconds": 15, "target_duration_tolerance_seconds": 4, "source_repo_path": "data/manim_docs_seed_cases.jsonl", "curation_decision": "rewrite", "curation_reason": "exact Manim 0.20.1 render produced only a still frame with zero animations; rewrite with a deliberate explanatory animation beat"}
{"case_id": "docs_counting_scene", "prompt": "Create a short Manim counting scene with a custom animation that updates a DecimalNumber from 0 to 100 while an updater keeps the number centered.", "completion": "from manim import *\n\nclass Count(Animation):\n    def __init__(self, number: DecimalNumber, start: float, end: float, **kwargs) -> None:\n        # Pass number as the mobject of the animation\n        super().__init__(number,  **kwargs)\n        # Set start and end\n        self.start = start\n        self.end = end\n\n    def interpolate_mobject(self, alpha: float) -> None:\n        # Set value of DecimalNumber according to alpha\n        value = self.start + (self.rate_func(alpha) * (self.end - self.start))\n        self.mobject.set_value(value)\n\n\nclass CountingScene(Scene):\n    def construct(self):\n        # Create Decimal Number and add it to scene\n        number = DecimalNumber().set_color(WHITE).scale(5)\n        # Add an updater to keep the DecimalNumber centered as its value changes\n        number.add_updater(lambda number: number.move_to(ORIGIN))\n\n        self.add(number)\n\n        self.wait()\n\n        # Play the Count Animation to count from 0 to 100 in 4 seconds\n        self.play(Count(number, 0, 100), run_time=4, rate_func=linear)\n\n        self.wait()", "entry_scene": "Count", "tags": ["docs", "building-blocks", "custom-animation", "updater", "10s", "tier:gold", "source:docs", "duration:10s"], "must_contain": ["class Count(Animation)", "DecimalNumber(", "add_updater(", "rate_func=linear"], "must_not_contain": ["Axes("], "source_name": "CountingScene", "source_url": "https://docs.manim.community/en/stable/tutorials/building_blocks.html", "source_anchor": "countingscene", "license": "MIT", "target_duration_seconds": 10, "target_duration_tolerance_seconds": 3, "source_repo_path": "data/manim_docs_feature_cases.jsonl", "curation_decision": "rewrite", "curation_reason": "entry_scene names Count Animation while the runnable scene is CountingScene"}
{"case_id": "docs_mobject_example", "prompt": "Create a short Manim scene that illustrates key point-access methods on a Line-based mobject, including the start, end, top, bottom, center, and a midpoint marker.", "completion": "from manim import *\n\nclass MobjectExample(Scene):\n    def construct(self):\n        p1 = [-1,-1, 0]\n        p2 = [ 1,-1, 0]\n        p3 = [ 1, 1, 0]\n        p4 = [-1, 1, 0]\n        a  = Line(p1,p2).append_points(Line(p2,p3).points).append_points(Line(p3,p4).points)\n        point_start  = a.get_start()\n        point_end    = a.get_end()\n        point_center = a.get_center()\n        self.add(Text(f\"a.get_start() = {np.round(point_start,2).tolist()}\", font_size=24).to_edge(UR).set_color(YELLOW))\n        self.add(Text(f\"a.get_end() = {np.round(point_end,2).tolist()}\", font_size=24).next_to(self.mobjects[-1],DOWN).set_color(RED))\n        self.add(Text(f\"a.get_center() = {np.round(point_center,2).tolist()}\", font_size=24).next_to(self.mobjects[-1],DOWN).set_color(BLUE))\n\n        self.add(Dot(a.get_start()).set_color(YELLOW).scale(2))\n        self.add(Dot(a.get_end()).set_color(RED).scale(2))\n        self.add(Dot(a.get_top()).set_color(GREEN_A).scale(2))\n        self.add(Dot(a.get_bottom()).set_color(GREEN_D).scale(2))\n        self.add(Dot(a.get_center()).set_color(BLUE).scale(2))\n        self.add(Dot(a.point_from_proportion(0.5)).set_color(ORANGE).scale(2))\n        self.add(*[Dot(x) for x in a.points])\n        self.add(a)", "entry_scene": "MobjectExample", "tags": ["docs", "building-blocks", "geometry", "coordinates", "10s", "tier:gold", "source:docs", "duration:10s"], "must_contain": ["Line(", "get_start()", "get_end()", "point_from_proportion("], "must_not_contain": ["ThreeDScene"], "source_name": "MobjectExample", "source_url": "https://docs.manim.community/en/stable/tutorials/building_blocks.html", "source_anchor": "mobjectexample", "license": "MIT", "target_duration_seconds": 10, "target_duration_tolerance_seconds": 3, "source_repo_path": "data/manim_docs_feature_cases.jsonl", "curation_decision": "rewrite", "curation_reason": "exact Manim 0.20.1 render produced only a still frame with zero animations; rewrite with a deliberate explanatory animation beat"}
{"case_id": "docs_threed_light_source_position", "prompt": "Create a short 3D Manim scene with axes and a shaded sphere surface to demonstrate a simple ThreeDScene setup and lighting-sensitive surface rendering.", "completion": "from manim import *\n\nclass ThreeDLightSourcePosition(ThreeDScene):\n    def construct(self):\n        axes = ThreeDAxes()\n        sphere = Surface(\n            lambda u, v: np.array([\n                1.5 * np.cos(u) * np.cos(v),\n                1.5 * np.cos(u) * np.sin(v),\n                1.5 * np.sin(u)\n            ]), v_range=[0, TAU], u_range=[-PI / 2, PI / 2],\n            checkerboard_colors=[RED_D, RED_E], resolution=(15, 32)\n        )\n        self.renderer.camera.light_source.move_to(3*IN) # changes the source of the light\n        self.set_camera_orientation(phi=75 * DEGREES, theta=30 * DEGREES)\n        self.add(axes, sphere)", "entry_scene": "ThreeDLightSourcePosition", "tags": ["docs", "3d", "surface", "light", "10s", "tier:gold", "source:docs", "duration:10s"], "must_contain": ["ThreeDScene", "ThreeDAxes()", "Surface(", "set_camera_orientation("], "must_not_contain": ["MovingCameraScene"], "source_name": "ThreeDLightSourcePosition", "source_url": "https://docs.manim.community/en/stable/examples.html", "source_anchor": "threedlightsourceposition", "license": "MIT", "target_duration_seconds": 10, "target_duration_tolerance_seconds": 3, "source_repo_path": "data/manim_docs_feature_cases.jsonl", "curation_decision": "rewrite", "curation_reason": "exact Manim 0.20.1 render produced only a still frame with zero animations; rewrite with a deliberate explanatory animation beat"}
{"case_id": "ml_backprop_signal_flow", "tags": ["curated", "ml", "backprop", "network", "longform", "tier:gold", "source:local", "duration:50s"], "entry_scene": "BackpropSignalFlow", "prompt": "Create a 50-second Manim scene explaining backpropagation in a small neural network. Show the forward pass from inputs to outputs, a loss node, backward gradient arrows, and a final parameter-update summary.", "must_contain": ["Circle(", "Arrow(", "DashedLine(", "Text("], "must_not_contain": ["ThreeDScene"], "target_duration_seconds": 50, "target_duration_tolerance_seconds": 8, "completion": "from manim import *\n\nclass BackpropSignalFlow(Scene):\n    def construct(self):\n        title = Text(\"Backpropagation Through a Small Network\", font_size=38).to_edge(UP)\n\n        inputs = VGroup(*[Circle(radius=0.18, color=BLUE_D, fill_opacity=0.18) for _ in range(3)]).arrange(DOWN, buff=0.55).shift(LEFT * 4)\n        hidden = VGroup(*[Circle(radius=0.18, color=TEAL_D, fill_opacity=0.18) for _ in range(4)]).arrange(DOWN, buff=0.5)\n        outputs = VGroup(*[Circle(radius=0.18, color=PURPLE_D, fill_opacity=0.18) for _ in range(2)]).arrange(DOWN, buff=0.8).shift(RIGHT * 4)\n        hidden.move_to(ORIGIN)\n        input_labels = VGroup(*[Text(label, font_size=22).next_to(node, LEFT) for label, node in zip([\"x1\", \"x2\", \"x3\"], inputs)])\n        output_labels = VGroup(*[Text(label, font_size=22).next_to(node, RIGHT) for label, node in zip([\"y1\", \"y2\"], outputs)])\n\n        forward_1 = VGroup(*[Arrow(inp.get_right(), hid.get_left(), buff=0.08, stroke_width=2.5, color=GRAY_B) for inp in inputs for hid in hidden])\n        forward_2 = VGroup(*[Arrow(hid.get_right(), out.get_left(), buff=0.08, stroke_width=2.5, color=GRAY_B) for hid in hidden for out in outputs])\n        loss_box = RoundedRectangle(corner_radius=0.1, width=1.4, height=0.7, color=RED_D, fill_opacity=0.14).next_to(outputs, RIGHT, buff=1.0)\n        loss_label = Text(\"loss\", font_size=26, color=RED_D).move_to(loss_box)\n        loss_arrows = VGroup(*[Arrow(out.get_right(), loss_box.get_left(), buff=0.08, stroke_width=3, color=RED_D) for out in outputs])\n\n        backward_2 = VGroup(*[Arrow(out.get_left(), hid.get_right(), buff=0.08, stroke_width=3, color=ORANGE, max_tip_length_to_length_ratio=0.2) for out in outputs for hid in hidden])\n        backward_1 = VGroup(*[Arrow(hid.get_left(), inp.get_right(), buff=0.08, stroke_width=3, color=YELLOW_D, max_tip_length_to_length_ratio=0.2) for hid in hidden for inp in inputs])\n        update_panel = VGroup(\n            RoundedRectangle(corner_radius=0.1, width=3.2, height=1.0, color=GREEN_D, fill_opacity=0.12),\n            Text(\"weights shift opposite the gradient\", font_size=24, color=GREEN_D),\n        ).arrange(DOWN, buff=0.18).to_edge(DOWN)\n\n        forward_caption = Text(\"Forward pass builds predictions\", font_size=28, color=GRAY_D).to_edge(DOWN)\n        loss_caption = Text(\"The loss compares prediction and target\", font_size=28, color=RED_D).to_edge(DOWN)\n        backward_caption = Text(\"Gradients flow backward to assign credit and blame\", font_size=28, color=ORANGE).to_edge(DOWN)\n\n        self.play(FadeIn(title), LaggedStart(*[FadeIn(node) for node in inputs], lag_ratio=0.1), LaggedStart(*[FadeIn(node) for node in hidden], lag_ratio=0.08), LaggedStart(*[FadeIn(node) for node in outputs], lag_ratio=0.12), FadeIn(input_labels), FadeIn(output_labels), run_time=5)\n        self.play(LaggedStart(*[GrowArrow(arrow) for arrow in forward_1], lag_ratio=0.04), run_time=4)\n        self.play(LaggedStart(*[GrowArrow(arrow) for arrow in forward_2], lag_ratio=0.05), FadeIn(forward_caption), run_time=4)\n        self.wait(1)\n        self.play(FadeIn(loss_box), FadeIn(loss_label), LaggedStart(*[GrowArrow(arrow) for arrow in loss_arrows], lag_ratio=0.15), ReplacementTransform(forward_caption, loss_caption), run_time=4)\n        self.wait(1)\n        self.play(LaggedStart(*[GrowArrow(arrow) for arrow in backward_2], lag_ratio=0.03), run_time=5)\n        self.play(LaggedStart(*[GrowArrow(arrow) for arrow in backward_1], lag_ratio=0.03), ReplacementTransform(loss_caption, backward_caption), run_time=5)\n        self.play(FadeIn(update_panel), run_time=3)\n        self.wait(3)\n", "license": "MIT", "source_repo_path": "data/manim_longform_cases.json", "curation_decision": "rewrite", "curation_reason": "required DashedLine API contract is absent from the completion"}
{"case_id": "linear_transformation_basis_story", "tags": ["underrepresented", "linear-transformation", "matrix", "math", "animation", "longform", "tier:gold", "source:local", "duration:30s"], "entry_scene": "LinearTransformationBasisStory", "prompt": "Create a 30-second Manim scene using LinearTransformationScene to explain how a matrix changes the basis vectors and a unit square. Keep the matrix visible, show a preview of the target shape, and use staged captions so the geometric effect is easy to follow.", "must_contain": ["LinearTransformationScene", "apply_matrix(", "IntegerMatrix(", "Square("], "must_not_contain": ["ThreeDScene"], "target_duration_seconds": 30, "target_duration_tolerance_seconds": 6, "completion": "from manim import *\n\nclass LinearTransformationBasisStory(LinearTransformationScene):\n    def __init__(self, **kwargs):\n        super().__init__(\n            show_coordinates=True,\n            leave_ghost_vectors=True,\n            show_basis_vectors=False,\n            background_plane_kwargs={\"background_line_style\": {\"stroke_opacity\": 0.3}},\n            **kwargs,\n        )\n\n    def construct(self):\n        matrix = [[1, 1], [-1, 2]]\n        title = Text(\"A Matrix Rebuilds the Basis\", font_size=34).to_edge(UP)\n        matrix_label = Text(\"matrix A\", font_size=22, color=GRAY_D).to_corner(UR)\n        matrix_display = IntegerMatrix(matrix).next_to(matrix_label, DOWN, buff=0.15)\n        square = Square(side_length=1.25, color=TEAL_D, fill_color=TEAL_E, fill_opacity=0.22).shift(RIGHT * 0.6 + UP * 0.55)\n        preview = square.copy().apply_matrix(matrix).set_stroke(YELLOW_D, opacity=0.65).set_fill(YELLOW_D, opacity=0.08)\n        i_vector = Vector([1, 0], color=GREEN_D)\n        j_vector = Vector([0, 1], color=BLUE_D)\n        i_label = always_redraw(lambda: MathTex(r\"\\\\hat{\\imath}\", color=GREEN_D).scale(0.8).next_to(i_vector.get_end(), DOWN + RIGHT, buff=0.12))\n        j_label = always_redraw(lambda: MathTex(r\"\\\\hat{\\jmath}\", color=BLUE_D).scale(0.8).next_to(j_vector.get_end(), UP + LEFT, buff=0.12))\n        caption_a = Text(\"The unit square and basis vectors describe the original coordinate frame.\", font_size=23, color=GRAY_D).to_edge(DOWN)\n        caption_b = Text(\"A preview helps separate the target geometry from the animation itself.\", font_size=23, color=GRAY_D).to_edge(DOWN)\n        caption_c = Text(\"Once the matrix acts, both the square and the basis vectors deform together.\", font_size=23, color=GRAY_D).to_edge(DOWN)\n\n        self.add_foreground_mobjects(title, matrix_label, matrix_display)\n        self.play(FadeIn(title), FadeIn(matrix_label), FadeIn(matrix_display), run_time=3)\n        self.play(Create(square), GrowArrow(i_vector), GrowArrow(j_vector), FadeIn(i_label), FadeIn(j_label), FadeIn(caption_a), run_time=4)\n        self.play(FadeIn(preview), ReplacementTransform(caption_a, caption_b), run_time=4)\n        self.play(FadeOut(preview), run_time=1)\n        self.add_transformable_mobject(square, i_vector, j_vector)\n        self.apply_matrix(matrix, run_time=6)\n        self.play(ReplacementTransform(caption_b, caption_c), Indicate(square, color=TEAL_A), Indicate(i_vector, color=GREEN_A), Indicate(j_vector, color=BLUE_A), run_time=4)\n        self.wait(2)\n", "license": "MIT", "source_repo_path": "data/manim_underrepresented_group_cases_round2.json", "curation_decision": "rewrite", "curation_reason": "fails to render on Manim 0.20.1 during apply_matrix"}
{"case_id": "code_pipeline_highlight_story", "tags": ["underrepresented", "code", "cs", "pipeline", "animation", "longform", "tier:gold", "source:local", "duration:30s"], "entry_scene": "CodePipelineHighlightStory", "prompt": "Create a 30-second Manim scene that shows a code snippet on the left and a three-stage pipeline diagram on the right. Move a highlight box across logical sections of the code while arrows and captions explain how the input, transform, and output stages relate.", "must_contain": ["Code(", "SurroundingRectangle(", "Arrow(", "ReplacementTransform("], "must_not_contain": ["ThreeDScene"], "target_duration_seconds": 30, "target_duration_tolerance_seconds": 6, "completion": "from manim import *\n\nclass CodePipelineHighlightStory(Scene):\n    def construct(self):\n        code_text = \"\"\"tokens = load_batch()\\nfeatures = encode(tokens)\\nsummary = pool(features)\\nlabel = classify(summary)\\nprint(label)\"\"\"\n        title = Text(\"Linking Code to a Visual Pipeline\", font_size=34).to_edge(UP)\n        code_panel = Code(code=code_text, tab_width=4, language=\"python\", background=\"window\", style=\"monokai\", insert_line_no=False).scale(0.58).to_edge(LEFT).shift(DOWN * 0.15)\n        code_frame = SurroundingRectangle(code_panel, color=GRAY_B, buff=0.12)\n        code_focus_a = RoundedRectangle(corner_radius=0.08, width=4.7, height=0.85, color=BLUE_D, stroke_width=3).move_to(code_panel.get_center() + UP * 1.0)\n        code_focus_b = RoundedRectangle(corner_radius=0.08, width=4.7, height=0.85, color=YELLOW_D, stroke_width=3).move_to(code_panel.get_center() + UP * 0.05)\n        code_focus_c = RoundedRectangle(corner_radius=0.08, width=4.7, height=1.1, color=GREEN_D, stroke_width=3).move_to(code_panel.get_center() + DOWN * 0.95)\n        stage_boxes = VGroup(*[\n            RoundedRectangle(corner_radius=0.12, width=2.25, height=1.0, color=color, fill_color=color, fill_opacity=0.16)\n            for color in [BLUE_D, YELLOW_D, GREEN_D]\n        ]).arrange(DOWN, buff=0.8).to_edge(RIGHT).shift(DOWN * 0.1)\n        stage_labels = VGroup(\n            Text(\"input\", font_size=24).move_to(stage_boxes[0]),\n            Text(\"transform\", font_size=24).move_to(stage_boxes[1]),\n            Text(\"output\", font_size=24).move_to(stage_boxes[2]),\n        )\n        arrows = VGroup(\n            Arrow(stage_boxes[0].get_bottom(), stage_boxes[1].get_top(), buff=0.12, color=GRAY_B),\n            Arrow(stage_boxes[1].get_bottom(), stage_boxes[2].get_top(), buff=0.12, color=GRAY_B),\n        )\n        stage_focus_a = SurroundingRectangle(stage_boxes[0], color=BLUE_D, buff=0.12)\n        stage_focus_b = SurroundingRectangle(stage_boxes[1], color=YELLOW_D, buff=0.12)\n        stage_focus_c = SurroundingRectangle(stage_boxes[2], color=GREEN_D, buff=0.12)\n        connector_arrow = Arrow(code_panel.get_right() + RIGHT * 0.15, stage_boxes[1].get_left() + LEFT * 0.15, buff=0.0, color=GRAY_C)\n        caption_a = Text(\"The first lines load and encode the input before any decision is made.\", font_size=23, color=GRAY_D).to_edge(DOWN)\n        caption_b = Text(\"The middle step compresses the representation into a usable summary.\", font_size=23, color=GRAY_D).to_edge(DOWN)\n        caption_c = Text(\"The final lines turn that summary into an output the pipeline can report.\", font_size=23, color=GRAY_D).to_edge(DOWN)\n\n        self.play(FadeIn(title), FadeIn(code_panel), Create(code_frame), FadeIn(stage_boxes), FadeIn(stage_labels), run_time=4)\n        self.play(GrowArrow(arrows[0]), GrowArrow(arrows[1]), GrowArrow(connector_arrow), Create(code_focus_a), Create(stage_focus_a), FadeIn(caption_a), run_time=4)\n        self.play(ReplacementTransform(code_focus_a, code_focus_b), ReplacementTransform(stage_focus_a, stage_focus_b), ReplacementTransform(caption_a, caption_b), run_time=5)\n        self.play(ReplacementTransform(code_focus_b, code_focus_c), ReplacementTransform(stage_focus_b, stage_focus_c), ReplacementTransform(caption_b, caption_c), run_time=5)\n        self.play(Indicate(stage_boxes[2], color=GREEN_A), Indicate(code_focus_c, color=GREEN_A), run_time=3)\n        self.wait(2)\n", "license": "MIT", "source_repo_path": "data/manim_underrepresented_group_cases_round2.json", "curation_decision": "rewrite", "curation_reason": "uses the obsolete Code(code=...) signature instead of Code(code_string=...)"}
{"case_id": "sphere_projection_orbit_story", "tags": ["3d", "camera", "animation", "projection", "movealongpath", "longform", "tier:gold", "source:local", "duration:20s"], "entry_scene": "SphereProjectionOrbitStory", "prompt": "Create a 20-second ThreeDScene with a sphere, a moving point on a latitude circle, and a projected shadow onto a plane below. Use camera motion and transition captions so the relationship between the 3D point and its projection is easy to follow.", "must_contain": ["ThreeDScene", "Surface(", "MoveAlongPath(", "always_redraw("], "must_not_contain": ["ZoomedScene"], "target_duration_seconds": 20, "target_duration_tolerance_seconds": 4, "completion": "from manim import *\n\nclass SphereProjectionOrbitStory(ThreeDScene):\n    def construct(self):\n        title = Text(\"A Projection Links 3D Motion to a Flat Shadow\", font_size=32).to_edge(UP)\n        self.add_fixed_in_frame_mobjects(title)\n        axes = ThreeDAxes(x_range=[-3, 3, 1], y_range=[-3, 3, 1], z_range=[-2, 3, 1])\n        sphere = Surface(lambda u, v: axes.c2p(1.4 * np.cos(u) * np.cos(v), 1.4 * np.cos(u) * np.sin(v), 1.4 * np.sin(u)), u_range=[-PI / 2, PI / 2], v_range=[0, TAU], checkerboard_colors=[BLUE_D, BLUE_E], resolution=(14, 20), fill_opacity=0.5)\n        orbit = ParametricFunction(lambda t: axes.c2p(1.2 * np.cos(t), 1.2 * np.sin(t), 0.7), t_range=[0, TAU], color=ORANGE, stroke_width=4)\n        tracker = ValueTracker(0.0)\n        point = always_redraw(lambda: Dot3D(axes.c2p(1.2 * np.cos(tracker.get_value()), 1.2 * np.sin(tracker.get_value()), 0.7), color=YELLOW, radius=0.08))\n        shadow = always_redraw(lambda: Dot3D(axes.c2p(1.2 * np.cos(tracker.get_value()), 1.2 * np.sin(tracker.get_value()), -1.1), color=TEAL_D, radius=0.07))\n        link = always_redraw(lambda: DashedLine(point.get_center(), shadow.get_center(), color=YELLOW_D, stroke_width=3))\n        plane = Surface(lambda u, v: axes.c2p(u, v, -1.1), u_range=[-2.1, 2.1], v_range=[-2.1, 2.1], checkerboard_colors=[GRAY_D, GRAY_E], resolution=(2, 2), fill_opacity=0.22)\n        caption_a = Text(\"The moving point lives on a raised orbit around the sphere.\", font_size=22, color=WHITE).to_edge(DOWN)\n        caption_b = Text(\"Its shadow on the plane below preserves the horizontal motion.\", font_size=22, color=WHITE).to_edge(DOWN)\n        self.add_fixed_in_frame_mobjects(caption_a)\n\n        self.set_camera_orientation(phi=68 * DEGREES, theta=-42 * DEGREES)\n        self.play(FadeIn(axes), FadeIn(plane), FadeIn(sphere), Create(orbit), FadeIn(point), FadeIn(shadow), Create(link), run_time=4)\n        self.play(tracker.animate.set_value(PI), run_time=4, rate_func=linear)\n        self.play(ReplacementTransform(caption_a, caption_b), run_time=2)\n        self.move_camera(phi=60 * DEGREES, theta=18 * DEGREES, run_time=3)\n        self.play(tracker.animate.set_value(TAU), run_time=4, rate_func=linear)\n        self.wait(1)\n", "license": "MIT", "source_repo_path": "data/manim_cross_category_expansion_cases.json", "curation_decision": "rewrite", "curation_reason": "required MoveAlongPath contract is absent and the review reports unreadable text"}
{"case_id": "robotics_path_planning_story", "tags": ["robotics", "planning", "animation", "comparison", "longform", "tier:gold", "source:local", "duration:30s"], "entry_scene": "RoboticsPathPlanningStory", "prompt": "Create a 30-second Manim scene that explains basic robot path planning around obstacles. Show start and goal points, obstacles, a naive blocked path, and then a planned curved path with captions.", "must_contain": ["Circle(", "VMobject(", "MoveAlongPath(", "ReplacementTransform("], "must_not_contain": ["ThreeDScene"], "target_duration_seconds": 30, "target_duration_tolerance_seconds": 6, "completion": "from manim import *\n\nclass RoboticsPathPlanningStory(Scene):\n    def construct(self):\n        title = Text(\"Planning Means Reaching the Goal Without Hitting Obstacles\", font_size=34).to_edge(UP)\n        start = Dot(LEFT * 4.0 + DOWN * 0.2, color=GREEN_D, radius=0.09)\n        goal = Dot(RIGHT * 4.0 + UP * 0.4, color=YELLOW_D, radius=0.09)\n        obstacles = VGroup(Circle(radius=0.65, color=RED_D, fill_opacity=0.2).shift(LEFT * 1.0 + UP * 0.6), Circle(radius=0.75, color=RED_D, fill_opacity=0.2).shift(RIGHT * 1.0 + DOWN * 0.4))\n        direct = DashedLine(start.get_center(), goal.get_center(), color=GRAY_B, stroke_width=4)\n        planned = VMobject(color=BLUE_D, stroke_width=5).set_points_smoothly([start.get_center(), LEFT * 2.6 + DOWN * 1.5, RIGHT * 0.2 + DOWN * 1.9, RIGHT * 2.3 + UP * 1.6, goal.get_center()])\n        robot = Dot(start.get_center(), color=BLUE_D, radius=0.08)\n        start_label = Text(\"start\", font_size=22, color=GREEN_D).next_to(start, DOWN, buff=0.12)\n        goal_label = Text(\"goal\", font_size=22, color=YELLOW_D).next_to(goal, UP, buff=0.12)\n        caption_a = Text(\"A direct route may collide with obstacles in the workspace.\", font_size=23, color=WHITE).to_edge(DOWN)\n        caption_b = Text(\"A planned path bends around obstacles while still reaching the goal.\", font_size=23, color=WHITE).to_edge(DOWN)\n        caption_c = Text(\"Good planning trades path simplicity for safety and feasibility.\", font_size=23, color=WHITE).to_edge(DOWN)\n\n        self.play(FadeIn(title), FadeIn(start), FadeIn(goal), FadeIn(start_label), FadeIn(goal_label), FadeIn(obstacles), FadeIn(caption_a), run_time=4)\n        self.play(Create(direct), run_time=3)\n        self.play(ReplacementTransform(direct, planned), ReplacementTransform(caption_a, caption_b), run_time=4)\n        self.play(FadeIn(robot), MoveAlongPath(robot, planned), ReplacementTransform(caption_b, caption_c), run_time=6, rate_func=linear)\n        self.wait(2)\n", "license": "MIT", "source_repo_path": "data/manim_domain_expansion_cases.json", "curation_decision": "rewrite", "curation_reason": "the claimed safe path intersects an obstacle"}
{"case_id": "threeb1b_discrete_to_curve_story", "tags": ["3b1b-style", "math", "probability", "animation", "longform", "tier:gold", "source:local", "duration:30s"], "entry_scene": "DiscreteToCurveStory", "prompt": "Create a 30-second Manim scene in a 3Blue1Brown-inspired style that starts with a handful of discrete sample bars and gradually suggests a smooth curve passing through the same trend. Make the scene feel like an explanatory reveal rather than a dashboard.", "must_contain": ["Axes(", "Rectangle(", "LaggedStart(", "VMobject("], "must_not_contain": ["ThreeDScene"], "target_duration_seconds": 30, "target_duration_tolerance_seconds": 6, "completion": "from manim import *\n\nclass DiscreteToCurveStory(Scene):\n    def construct(self):\n        title = Text(\"Discrete Pieces Can Hint at a Smooth Law\", font_size=35).to_edge(UP)\n        axes = Axes(x_range=[0, 8, 1], y_range=[0, 3.2, 0.5], x_length=8.2, y_length=4.8, tips=False).shift(DOWN * 0.2)\n        sample_x = [0.8, 1.8, 2.8, 3.8, 4.8, 5.8, 6.8]\n        sample_y = [0.55, 1.0, 1.65, 2.2, 2.45, 2.05, 1.35]\n        bars = VGroup()\n        dots = VGroup()\n        for x, y in zip(sample_x, sample_y, strict=True):\n            width = axes.c2p(0.7, 0)[0] - axes.c2p(0, 0)[0]\n            height = axes.c2p(0, y)[1] - axes.c2p(0, 0)[1]\n            bar = Rectangle(width=width, height=height, stroke_color=BLUE_D, fill_color=BLUE_D, fill_opacity=0.22)\n            bar.move_to(axes.c2p(x, y / 2))\n            bars.add(bar)\n            dots.add(Dot(axes.c2p(x, y), color=YELLOW, radius=0.07))\n        smooth_curve = axes.plot(lambda x: float(np.interp(x, sample_x, sample_y)), x_range=[sample_x[0], sample_x[-1]], color=TEAL_D)\n        area = axes.get_area(smooth_curve, x_range=(sample_x[0], sample_x[-1]), color=TEAL_D, opacity=0.18)\n        caption_a = Text(\"At first we only see separate chunks of information.\", font_size=24, color=WHITE).to_edge(DOWN)\n        caption_b = Text(\"The dots suggest a pattern that the bars alone only hint at.\", font_size=24, color=WHITE).to_edge(DOWN)\n        caption_c = Text(\"A smooth curve turns those pieces into one continuous story.\", font_size=24, color=WHITE).to_edge(DOWN)\n\n        self.play(FadeIn(title), Create(axes), run_time=4)\n        self.play(LaggedStart(*[GrowFromEdge(bar, DOWN) for bar in bars], lag_ratio=0.14), FadeIn(caption_a), run_time=5)\n        self.play(LaggedStart(*[FadeIn(dot) for dot in dots], lag_ratio=0.12), ReplacementTransform(caption_a, caption_b), run_time=4)\n        self.play(Create(smooth_curve), FadeIn(area), ReplacementTransform(caption_b, caption_c), run_time=5)\n        self.wait(2)\n", "license": "MIT", "source_repo_path": "data/manim_3b1b_style_cases.json", "curation_decision": "rewrite", "curation_reason": "required VMobject contract is absent and the review reports unreadable text"}
{"case_id": "candidate_code_matrix_attention_trace", "prompt": "Create a 22-second Manim scene that traces one attention step from code to matrix output. Show a short Python-like pseudocode block, a 3x3 score matrix, highlight one query row, copy that row into a weighted value vector, and end with a compact caption that connects the code line to the matrix operation.", "completion": "from manim import *\n\nclass CodeMatrixAttentionTrace(Scene):\n    def construct(self):\n        title = Text(\"Attention: one query row\", font_size=34).to_edge(UP)\n        snippet = Code(\n            code_string=\"scores = Q @ K.T\\nweights = softmax(scores)\\nout = weights @ V\",\n            language=\"Python\",\n            formatter_style=\"monokai\",\n            add_line_numbers=False,\n            background=\"window\",\n        ).scale(0.62).to_edge(LEFT, buff=0.55).shift(UP * 0.2)\n        scores = Matrix([[\"0.1\", \"1.4\", \"0.3\"], [\"0.7\", \"0.2\", \"1.1\"], [\"1.2\", \"0.4\", \"0.5\"]]).scale(0.62).shift(RIGHT * 1.1 + UP * 0.35)\n        values = Matrix([[\"v_1\"], [\"v_2\"], [\"v_3\"]]).scale(0.62).next_to(scores, RIGHT, buff=0.9)\n        row_box = SurroundingRectangle(scores.get_rows()[1], color=YELLOW_D, buff=0.08)\n        value_box = SurroundingRectangle(values, color=GREEN_D, buff=0.1)\n        row_label = Text(\"query 2 weights\", font_size=22, color=YELLOW_D).next_to(row_box, DOWN, buff=0.18)\n        out_token = VGroup(\n            RoundedRectangle(corner_radius=0.08, width=1.3, height=0.7, color=GREEN_D, fill_color=GREEN_E, fill_opacity=0.25),\n            MathTex(r\"out_2\", color=GREEN_D),\n        ).arrange().next_to(values, DOWN, buff=0.55)\n        caption = Text(\"The highlighted code line becomes a weighted sum over value vectors.\", font_size=22, color=WHITE).to_edge(DOWN)\n        caption_panel = VGroup(BackgroundRectangle(caption, fill_opacity=0.86, buff=0.16), caption)\n\n        self.play(FadeIn(title), FadeIn(snippet), run_time=3)\n        self.play(FadeIn(scores), FadeIn(values), run_time=3)\n        self.play(Create(row_box), FadeIn(row_label), run_time=2.5)\n        self.play(Create(value_box), TransformFromCopy(scores.get_rows()[1], out_token), run_time=4)\n        self.play(FadeIn(caption_panel), Indicate(out_token, color=GREEN_A), run_time=3)\n        self.wait(2)\n", "entry_scene": "CodeMatrixAttentionTrace", "tags": ["ml", "attention", "code", "matrix", "20s", "tier:silver", "status:approved", "source:review-promotion", "source:local", "duration:20s"], "must_contain": ["Code(", "Matrix(", "SurroundingRectangle(", "TransformFromCopy("], "must_not_contain": ["manim_ml", "manim_chemistry"], "source_name": "Codex Simula-inspired mechanism design batch 11", "source_domain": "machine-learning", "source_repo_path": "data/manim_review_candidates_round11_mechanism_design.jsonl", "uses_custom_library": false, "is_plain_manim_candidate": true, "requires_manual_conversion": false, "target_duration_seconds": 22, "target_duration_tolerance_seconds": 5, "license": "MIT", "curation_decision": "rewrite", "curation_reason": "copies a score row into the output without teaching the weighted value sum"}
{"case_id": "attention_weighted_average_micro", "prompt": "Create a 15-second Manim scene that explains attention as a weighted average. Show three value vectors, three attention weights, copy the strongest weight into a highlighted output token, and keep a small legend visible.", "completion": "from manim import *\n\nclass AttentionWeightedAverageMicro(Scene):\n    def construct(self):\n        title = Text(\"Attention builds a weighted average\", font_size=32).to_edge(UP)\n        weights = Matrix([[\"0.15\", \"0.70\", \"0.15\"]]).scale(0.72).shift(UP * 1.2)\n        values = VGroup(*[Matrix([[label], [value]]).scale(0.55) for label, value in [(\"v_1\", \"cat\"), (\"v_2\", \"purr\"), (\"v_3\", \"tail\")]]).arrange(RIGHT, buff=0.75).shift(DOWN * 0.45)\n        weight_label = Text(\"attention weights\", font_size=22, color=GRAY_B).next_to(weights, UP, buff=0.15)\n        value_label = Text(\"value vectors\", font_size=22, color=GRAY_B).next_to(values, DOWN, buff=0.18)\n        strong_box = SurroundingRectangle(weights.get_columns()[1], color=YELLOW_D, buff=0.08)\n        value_box = SurroundingRectangle(values[1], color=YELLOW_D, buff=0.1)\n        output = VGroup(RoundedRectangle(corner_radius=0.08, width=1.6, height=0.75, color=GREEN_D, fill_color=GREEN_E, fill_opacity=0.25), MathTex(\"out\", font_size=34, color=GREEN_D)).arrange().to_edge(RIGHT, buff=0.75).shift(UP * 0.2)\n        arrow = Arrow(values[1].get_right(), output.get_left(), buff=0.18, color=GREEN_D)\n        legend = Text(\"larger weight means more influence\", font_size=23, color=WHITE).to_edge(DOWN)\n\n        self.play(FadeIn(title), FadeIn(weights), FadeIn(weight_label), run_time=3)\n        self.play(FadeIn(values), FadeIn(value_label), run_time=3)\n        self.play(Create(strong_box), Create(value_box), run_time=2)\n        self.play(TransformFromCopy(values[1], output), GrowArrow(arrow), FadeIn(legend), run_time=4)\n        self.wait(3)\n", "entry_scene": "AttentionWeightedAverageMicro", "tags": ["learning-app", "taste", "ml", "attention", "duration:15s", "tier:silver", "status:approved", "source:review-promotion", "source:local", "duration:20s"], "must_contain": ["Matrix(", "MathTex(", "Arrow(", "TransformFromCopy(", "SurroundingRectangle("], "must_not_contain": ["ThreeDScene", "SVGMobject"], "license": "MIT", "source_name": "learning_app_quality_review_round12", "source_domain": "local", "source_repo_path": "docs/learning-app-quality-review.md", "uses_custom_library": false, "is_plain_manim_candidate": true, "requires_manual_conversion": false, "target_duration_seconds": 15, "target_duration_tolerance_seconds": 3, "curation_decision": "rewrite", "curation_reason": "claims a weighted average but copies only one value to the output"}
{"case_id": "round13_bayes_update_weight_shift", "prompt": "Create a 15-second Manim scene that explains a Bayesian update. Show prior hypothesis weights as bars, animate them into posterior weights after evidence, and add one takeaway caption.", "completion": "from manim import *\n\nclass BayesUpdateWeightShift(Scene):\n    def construct(self):\n        title = Text(\"Evidence shifts belief weights\", font_size=34).to_edge(UP)\n        prior = BarChart(values=[0.5, 0.3, 0.2], bar_names=[\"A\", \"B\", \"C\"], y_range=[0, 0.7, 0.1], y_length=3.3, x_length=4.7, bar_colors=[BLUE_D, GRAY_B, GRAY_B]).shift(LEFT * 2.3 + DOWN * 0.15)\n        posterior = BarChart(values=[0.25, 0.58, 0.17], bar_names=[\"A\", \"B\", \"C\"], y_range=[0, 0.7, 0.1], y_length=3.3, x_length=4.7, bar_colors=[GRAY_B, GREEN_D, GRAY_B]).shift(RIGHT * 2.3 + DOWN * 0.15)\n        prior_label = Text(\"prior\", font_size=25).next_to(prior, DOWN)\n        post_label = Text(\"posterior\", font_size=25, color=GREEN_D).next_to(posterior, DOWN)\n        evidence = MathTex(r\"P(H\\mid E)\", color=YELLOW_D).to_edge(DOWN)\n        arrow = Arrow(prior.get_right(), posterior.get_left(), color=YELLOW_D, buff=0.25)\n        self.play(FadeIn(title), FadeIn(prior), FadeIn(prior_label), run_time=3)\n        self.play(FadeIn(evidence), GrowArrow(arrow), run_time=2)\n        self.play(FadeIn(posterior), FadeIn(post_label), run_time=3)\n        self.play(Indicate(posterior.bars[1], color=GREEN_A), run_time=2)\n        self.wait(1)\n", "entry_scene": "BayesUpdateWeightShift", "tags": ["round13", "learning-app", "bayes", "bar-chart", "duration:15s", "tier:silver", "status:approved", "source:review-promotion", "source:local", "duration:20s"], "must_contain": ["BarChart(", "MathTex(", "Arrow(", "Indicate("], "must_not_contain": ["manim_ml", "manim_chemistry"], "source_name": "Codex Round 13 public quality expansion", "source_domain": "statistics", "source_repo_path": "data/manim_review_candidates_round13_public_quality.jsonl", "uses_custom_library": false, "is_plain_manim_candidate": true, "requires_manual_conversion": false, "target_duration_seconds": 15, "target_duration_tolerance_seconds": 4, "license": "MIT", "curation_decision": "rewrite", "curation_reason": "overlapping charts and no visible prior-likelihood-evidence update mechanism"}
{"case_id": "round13_attention_weighted_sum_tokens", "prompt": "Create a 15-second Manim scene that explains attention as a weighted sum. Show three value tokens, three weights, highlight the largest weight, and copy weighted value pieces into one output token.", "completion": "from manim import *\n\nclass AttentionWeightedSumTokens(Scene):\n    def construct(self):\n        title = Text(\"Attention mixes values by weight\", font_size=34).to_edge(UP)\n        values = VGroup(*[RoundedRectangle(width=1.1, height=0.65, corner_radius=0.08, color=c) for c in [BLUE_D, GREEN_D, PURPLE_D]]).arrange(RIGHT, buff=0.45).shift(UP * 0.75)\n        labels = VGroup(*[MathTex(f\"v_{i+1}\").move_to(values[i]) for i in range(3)])\n        weights = VGroup(MathTex(\"0.15\"), MathTex(\"0.70\", color=YELLOW_D), MathTex(\"0.15\")).arrange(RIGHT, buff=0.75).next_to(values, DOWN, buff=0.35)\n        box = SurroundingRectangle(weights[1], color=YELLOW_D, buff=0.08)\n        output = VGroup(RoundedRectangle(width=1.35, height=0.75, corner_radius=0.08, color=YELLOW_D, fill_color=YELLOW_E, fill_opacity=0.22), MathTex(\"out\")).arrange().shift(DOWN * 1.45)\n        arrows = VGroup(*[Arrow(values[i].get_bottom(), output.get_top(), color=GRAY_B, buff=0.12) for i in range(3)])\n        caption = Text(\"The strongest weight contributes most to the output.\", font_size=24).to_edge(DOWN)\n        self.play(FadeIn(title), FadeIn(values), FadeIn(labels), run_time=2.5)\n        self.play(FadeIn(weights), Create(box), run_time=2)\n        self.play(LaggedStart(*[GrowArrow(a) for a in arrows], lag_ratio=0.15), run_time=2.5)\n        self.play(TransformFromCopy(values[1], output), FadeIn(caption), run_time=3)\n        self.wait(1)\n", "entry_scene": "AttentionWeightedSumTokens", "tags": ["round13", "learning-app", "attention", "matrix", "duration:15s", "tier:silver", "status:approved", "source:review-promotion", "source:local", "duration:20s"], "must_contain": ["RoundedRectangle(", "MathTex(", "SurroundingRectangle(", "TransformFromCopy("], "must_not_contain": ["manim_ml", "manim_chemistry"], "source_name": "Codex Round 13 public quality expansion", "source_domain": "machine-learning", "source_repo_path": "data/manim_review_candidates_round13_public_quality.jsonl", "uses_custom_library": false, "is_plain_manim_candidate": true, "requires_manual_conversion": false, "target_duration_seconds": 15, "target_duration_tolerance_seconds": 4, "license": "MIT", "curation_decision": "rewrite", "curation_reason": "claims a weighted sum but copies only one value to the output"}
{"case_id": "round13_confidence_interval_widths", "prompt": "Create a 15-second Manim scene that explains confidence interval width. Show an estimate on a number line, expand a narrow interval and a wide interval, then compare precision with a short caption.", "completion": "from manim import *\n\nclass ConfidenceIntervalWidths(Scene):\n    def construct(self):\n        title = Text(\"Interval width shows uncertainty\", font_size=34).to_edge(UP)\n        line = NumberLine(x_range=[0, 10, 1], length=8, include_numbers=True).shift(UP * 0.35)\n        estimate = Dot(line.n2p(5), color=YELLOW_D)\n        narrow = Line(line.n2p(4.4), line.n2p(5.6), color=GREEN_D, stroke_width=12).shift(DOWN * 0.35)\n        wide = Line(line.n2p(2.7), line.n2p(7.3), color=BLUE_D, stroke_width=12).shift(DOWN * 1.05)\n        brace = BraceBetweenPoints(wide.get_start(), wide.get_end(), DOWN, color=BLUE_D)\n        labels = VGroup(Text(\"narrow: more precise\", font_size=22, color=GREEN_D).next_to(narrow, RIGHT), Text(\"wide: more uncertain\", font_size=22, color=BLUE_D).next_to(wide, RIGHT))\n        caption = Text(\"Same estimate, different uncertainty around it.\", font_size=24).to_edge(DOWN)\n        self.play(FadeIn(title), Create(line), FadeIn(estimate), run_time=2.5)\n        self.play(Create(narrow), FadeIn(labels[0]), run_time=2)\n        self.play(Create(wide), GrowFromCenter(brace), FadeIn(labels[1]), run_time=3)\n        self.play(FadeIn(caption), Indicate(estimate, color=YELLOW_A), run_time=2)\n        self.wait(1)\n", "entry_scene": "ConfidenceIntervalWidths", "tags": ["round13", "learning-app", "confidence-interval", "number-line", "duration:15s", "tier:silver", "status:approved", "source:review-promotion", "source:local", "duration:20s"], "must_contain": ["NumberLine(", "BraceBetweenPoints(", "Line(", "Dot("], "must_not_contain": ["manim_ml", "manim_chemistry"], "source_name": "Codex Round 13 public quality expansion", "source_domain": "statistics", "source_repo_path": "data/manim_review_candidates_round13_public_quality.jsonl", "uses_custom_library": false, "is_plain_manim_candidate": true, "requires_manual_conversion": false, "target_duration_seconds": 15, "target_duration_tolerance_seconds": 4, "license": "MIT", "curation_decision": "rewrite", "curation_reason": "narrow and wide interval labels overlap"}
{"case_id": "round13_conv_kernel_patch_slide", "prompt": "Create a 15-second Manim scene explaining a convolution kernel. Show a small image grid, a 3x3 kernel window, slide it one step, and emit one output cell.", "completion": "from manim import *\n\nclass ConvKernelPatchSlide(Scene):\n    def construct(self):\n        title = Text(\"A convolution reuses one small window\", font_size=32).to_edge(UP)\n        grid = VGroup(*[Square(0.42, color=GRAY_B) for _ in range(25)]).arrange_in_grid(5, 5, buff=0).shift(LEFT * 1.7 + UP * 0.1)\n        rows = [0, 1, 2]\n        cols = [0, 1, 2]\n        patch = VGroup(*[grid[r * 5 + c] for r in rows for c in cols])\n        window = SurroundingRectangle(patch, color=YELLOW_D, buff=0.03)\n        output = VGroup(Square(0.62, color=GREEN_D, fill_color=GREEN_E, fill_opacity=0.35), Text(\"out\", font_size=20)).shift(RIGHT * 2.4)\n        arrow = Arrow(window.get_right(), output.get_left(), color=GREEN_D, buff=0.18)\n        caption = Text(\"The same kernel scans local patches into output cells.\", font_size=24).to_edge(DOWN)\n        self.play(FadeIn(title), FadeIn(grid), Create(window), run_time=3)\n        self.play(window.animate.shift(RIGHT * 0.42), run_time=2)\n        self.play(GrowArrow(arrow), FadeIn(output), run_time=2.5)\n        self.play(FadeIn(caption), Indicate(output, color=GREEN_A), run_time=2)\n        self.wait(1)\n", "entry_scene": "ConvKernelPatchSlide", "tags": ["round13", "ml", "convolution", "duration:15s", "tier:silver", "status:approved", "source:review-promotion", "source:local", "duration:20s"], "must_contain": ["Square(", "SurroundingRectangle(", "Arrow(", "arrange_in_grid("], "must_not_contain": ["manim_ml", "manim_chemistry"], "source_name": "Codex Round 13 public quality expansion", "source_domain": "machine-learning", "source_repo_path": "data/manim_review_candidates_round13_public_quality.jsonl", "uses_custom_library": false, "is_plain_manim_candidate": true, "requires_manual_conversion": false, "target_duration_seconds": 15, "target_duration_tolerance_seconds": 4, "license": "MIT", "curation_decision": "rewrite", "curation_reason": "moves a patch without showing the kernel computation or numeric output"}
{"case_id": "round13_code_loop_accumulator_trace", "prompt": "Create a 15-second Manim scene that traces a loop accumulator. Show three input numbers, a running total box, and arrows from each number into the total.", "completion": "from manim import *\n\nclass CodeLoopAccumulatorTrace(Scene):\n    def construct(self):\n        title = Text(\"A loop updates one running total\", font_size=32).to_edge(UP)\n        code = Code(code_string=\"total = 0\\nfor x in [2, 3, 5]:\\n    total += x\", language=\"Python\", add_line_numbers=False, background=\"window\").scale(0.58).to_edge(LEFT)\n        nums = VGroup(*[VGroup(Circle(radius=0.28, color=BLUE_D), Text(str(n), font_size=22)) for n in [2, 3, 5]]).arrange(RIGHT, buff=0.4).shift(RIGHT * 1.8 + UP * 0.9)\n        for num in nums:\n            num[1].move_to(num[0])\n        total = VGroup(RoundedRectangle(width=1.7, height=0.75, corner_radius=0.08, color=YELLOW_D), MathTex(\"total=10\", color=YELLOW_D)).shift(RIGHT * 1.8 + DOWN * 0.75)\n        total[1].move_to(total[0])\n        arrows = VGroup(*[Arrow(num.get_bottom(), total.get_top(), color=GRAY_B, buff=0.1) for num in nums])\n        caption = Text(\"Each iteration folds one value into the same state.\", font_size=24).to_edge(DOWN)\n        self.play(FadeIn(title), FadeIn(code), FadeIn(nums), run_time=3)\n        self.play(LaggedStart(*[GrowArrow(a) for a in arrows], lag_ratio=0.2), run_time=3)\n        self.play(FadeIn(total), FadeIn(caption), run_time=2.5)\n        self.wait(1)\n", "entry_scene": "CodeLoopAccumulatorTrace", "tags": ["round13", "code", "programming", "duration:15s", "tier:silver", "status:approved", "source:review-promotion", "source:local", "duration:20s"], "must_contain": ["Code(", "Circle(", "RoundedRectangle(", "LaggedStart("], "must_not_contain": ["manim_ml", "manim_chemistry"], "source_name": "Codex Round 13 public quality expansion", "source_domain": "computer-science", "source_repo_path": "data/manim_review_candidates_round13_public_quality.jsonl", "uses_custom_library": false, "is_plain_manim_candidate": true, "requires_manual_conversion": false, "target_duration_seconds": 15, "target_duration_tolerance_seconds": 4, "license": "MIT", "curation_decision": "rewrite", "curation_reason": "shows the final total without tracing intermediate accumulator states"}
{"case_id": "round13_circuit_capacitor_charge", "prompt": "Create a 15-second Manim scene showing a capacitor charging. Draw a simple circuit path, a capacitor gap, moving charge dots, and a voltage label that increases.", "completion": "from manim import *\n\nclass CircuitCapacitorCharge(Scene):\n    def construct(self):\n        title = Text(\"A capacitor stores separated charge\", font_size=32).to_edge(UP)\n        wire = VGroup(Line(LEFT * 3, LEFT * 0.6), Line(RIGHT * 0.6, RIGHT * 3), Line(LEFT * 3, LEFT * 3 + DOWN * 1.6), Line(RIGHT * 3, RIGHT * 3 + DOWN * 1.6), Line(LEFT * 3 + DOWN * 1.6, RIGHT * 3 + DOWN * 1.6))\n        plates = VGroup(Line(LEFT * 0.45 + UP * 0.8, LEFT * 0.45 + DOWN * 0.8, color=YELLOW_D), Line(RIGHT * 0.45 + UP * 0.8, RIGHT * 0.45 + DOWN * 0.8, color=YELLOW_D))\n        dots = VGroup(*[Dot(LEFT * (1.8 - 0.35 * i), color=BLUE_D, radius=0.06) for i in range(4)])\n        voltage = MathTex(\"V\", color=YELLOW_D).next_to(plates, UP)\n        caption = Text(\"Charge separation creates voltage across the plates.\", font_size=24).to_edge(DOWN)\n        self.play(FadeIn(title), Create(wire), Create(plates), run_time=3)\n        self.play(LaggedStart(*[dot.animate.shift(RIGHT * 1.0) for dot in dots], lag_ratio=0.15), run_time=3)\n        self.play(FadeIn(voltage), FadeIn(caption), run_time=2.5)\n        self.wait(1)\n", "entry_scene": "CircuitCapacitorCharge", "tags": ["round13", "engineering", "circuit", "capacitor", "duration:15s", "tier:silver", "status:approved", "source:review-promotion", "source:local", "duration:20s"], "must_contain": ["Line(", "Dot(", "MathTex(", "LaggedStart("], "must_not_contain": ["manim_ml", "manim_chemistry"], "source_name": "Codex Round 13 public quality expansion", "source_domain": "engineering", "source_repo_path": "data/manim_review_candidates_round13_public_quality.jsonl", "uses_custom_library": false, "is_plain_manim_candidate": true, "requires_manual_conversion": false, "target_duration_seconds": 15, "target_duration_tolerance_seconds": 4, "license": "MIT", "curation_decision": "rewrite", "curation_reason": "prompt requests an increasing voltage readout but the label remains static"}
{"case_id": "round13_supply_demand_tax_wedge", "prompt": "Create a 15-second Manim scene that shows a tax wedge in supply and demand. Plot two lines, mark equilibrium, add a vertical wedge between buyer and seller prices, and caption the deadweight gap.", "completion": "from manim import *\n\nclass SupplyDemandTaxWedge(Scene):\n    def construct(self):\n        title = Text(\"A tax wedge separates buyer and seller prices\", font_size=30).to_edge(UP)\n        axes = Axes(x_range=[0, 6, 1], y_range=[0, 6, 1], x_length=5.8, y_length=4.1, tips=False).shift(DOWN * 0.25)\n        demand = axes.plot(lambda x: 5.2 - 0.55 * x, x_range=[0.5, 5.5], color=BLUE_D)\n        supply = axes.plot(lambda x: 1.0 + 0.55 * x, x_range=[0.5, 5.5], color=GREEN_D)\n        eq = Dot(axes.c2p(3.8, 3.1), color=YELLOW_D)\n        wedge = DoubleArrow(axes.c2p(3.0, 2.65), axes.c2p(3.0, 3.85), color=RED_D, buff=0)\n        label = Text(\"tax wedge\", font_size=22, color=RED_D).next_to(wedge, RIGHT)\n        caption = Text(\"The traded quantity shrinks when the wedge opens.\", font_size=24).to_edge(DOWN)\n        self.play(FadeIn(title), Create(axes), Create(demand), Create(supply), run_time=3)\n        self.play(FadeIn(eq), run_time=1.5)\n        self.play(GrowFromCenter(wedge), FadeIn(label), run_time=2.5)\n        self.play(FadeIn(caption), Indicate(wedge, color=RED_A), run_time=2)\n        self.wait(1)\n", "entry_scene": "SupplyDemandTaxWedge", "tags": ["round13", "economics", "supply-demand", "duration:15s", "tier:silver", "status:approved", "source:review-promotion", "source:local", "duration:20s"], "must_contain": ["Axes(", ".plot(", "DoubleArrow(", "Dot("], "must_not_contain": ["manim_ml", "manim_chemistry"], "source_name": "Codex Round 13 public quality expansion", "source_domain": "economics", "source_repo_path": "data/manim_review_candidates_round13_public_quality.jsonl", "uses_custom_library": false, "is_plain_manim_candidate": true, "requires_manual_conversion": false, "target_duration_seconds": 15, "target_duration_tolerance_seconds": 4, "license": "MIT", "curation_decision": "rewrite", "curation_reason": "prompt requests the deadweight gap but the completion does not show it"}
{"case_id": "round13_enzyme_activation_energy", "prompt": "Create a 15-second Manim scene explaining how an enzyme lowers activation energy. Plot two energy curves, mark the lower peak, and show a reaction arrow moving over the lower barrier.", "completion": "from manim import *\n\nclass EnzymeActivationEnergy(Scene):\n    def construct(self):\n        title = Text(\"Enzymes lower the activation barrier\", font_size=31).to_edge(UP)\n        axes = Axes(x_range=[0, 5, 1], y_range=[0, 4, 1], x_length=6.5, y_length=3.8, tips=False).shift(DOWN * 0.25)\n        uncatalyzed = axes.plot(lambda x: 0.8 + 2.7 * np.exp(-((x - 2.4) ** 2) / 0.45), x_range=[0.4, 4.6], color=RED_D)\n        catalyzed = axes.plot(lambda x: 0.8 + 1.45 * np.exp(-((x - 2.4) ** 2) / 0.75), x_range=[0.4, 4.6], color=GREEN_D)\n        lower_peak = Dot(axes.c2p(2.4, 2.25), color=GREEN_D)\n        label = Text(\"lower peak\", font_size=22, color=GREEN_D).next_to(lower_peak, RIGHT)\n        caption = Text(\"The reaction path becomes easier to cross.\", font_size=24).to_edge(DOWN)\n        self.play(FadeIn(title), Create(axes), Create(uncatalyzed), run_time=3)\n        self.play(Create(catalyzed), FadeIn(lower_peak), FadeIn(label), run_time=3)\n        self.play(FadeIn(caption), Indicate(lower_peak, color=GREEN_A), run_time=2)\n        self.wait(1)\n", "entry_scene": "EnzymeActivationEnergy", "tags": ["round13", "chemistry", "enzyme", "duration:15s", "tier:silver", "status:approved", "source:review-promotion", "source:local", "duration:20s"], "must_contain": ["Axes(", ".plot(", "Dot(", "Indicate("], "must_not_contain": ["manim_ml", "manim_chemistry"], "source_name": "Codex Round 13 public quality expansion", "source_domain": "chemistry", "source_repo_path": "data/manim_review_candidates_round13_public_quality.jsonl", "uses_custom_library": false, "is_plain_manim_candidate": true, "requires_manual_conversion": false, "target_duration_seconds": 15, "target_duration_tolerance_seconds": 4, "license": "MIT", "curation_decision": "rewrite", "curation_reason": "prompt requests a reaction arrow moving over the lower barrier but no arrow is present"}
